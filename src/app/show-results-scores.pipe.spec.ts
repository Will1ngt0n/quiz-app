import { ShowResultsScoresPipe } from './show-results-scores.pipe';

describe('ShowResultsScoresPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowResultsScoresPipe();
    expect(pipe).toBeTruthy();
  });
});
